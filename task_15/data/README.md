# Task 15 data

This folder contains both final outputs and archived outputs.

## Final

The `data/` folder contains only the plots used in the report and other similar ones
 
## Archive

All `_tmp_*`, `_sanity_*`, and intermediate runs were moved to:
- `data/_archive_2026-02-02/`

This keeps `data/` clean without deleting any work.
